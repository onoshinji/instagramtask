<!DOCTYPE html>
<html>
<div class="container-fluid">
  <head>
    <title>インスタグラムクローン</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width">  <!-- head内にこのタグ設定を追記する -->
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  </head>
  <header>
    <div class = "text-right header_text">
      <!-- ログインしているかどうかで処理を分ける -->
      <p class="fa fa-instagram fa-fw"></p>
      <% if logged_in? %>
        <%= "ログイン中: #{current_user.name}さん" %>
        <%= link_to "Profile", user_path(current_user.id) %>
        <%= link_to "Logout", session_path(current_user.id), method: :delete %>
        <% else %>
        <%= "あなたは今ログインしていません" %>
        <%= link_to "Sign up", new_user_path %>
        <%= link_to "Login", new_session_path %>
      <% end %>
      <a><i class="fa fa-facebook fa-fw"></i>&nbsp; facebook</a>
      <a><i class="fa fa-google fa-fw"></i>&nbsp; google</a>
      <a><i class="fa fa-twitter fa-fw"></i>&nbsp; twitter</a>
      <a><i class="fa fa-instagram fa-fw"></i>&nbsp; instagram</a>
      </div>
    </header>
    <div id class ="blend">
      <body>
        <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
        <% end %>
        <%= yield %>
        <footer>
          <p>インスタグラムクローン</p>
          <p>© 2020 5/27 ~ 5/31 ５月期小野作成</p>
        </footer>
      </body>
    </div>
</div>
</html>
