<!DOCTYPE html>
<html>
<div class="container-fluid">
  <head>
    <title>インスタグラムクローン</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width">  <!-- head内にこのタグ設定を追記する -->
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  </head>
  <header>
    <div class = "text-right header_text">
      <!-- ログインしているかどうかで処理を分ける -->
      <% if logged_in? %>
      <span class="fa fa-facebook-square"</span>
        <%= "#{current_user.name}" %>
        <%= link_to "Profile", user_path(current_user.id) %>
        <%= link_to "Logout", session_path(current_user.id), method: :delete %>
        <% else %>
        <%= "あなたは今ログインしていません" %>
        <%= link_to "Sign up", new_user_path %>
        <%= link_to "Login", new_session_path %>
        <% end %>
        <a ><i class="fa fa-home fa-fw"></i>&nbsp; Home</a>
        <a><i class="fa fa-book fa-fw"></i>&nbsp; Library</a>
        <a ><i class="fa fa-pencil fa-fw"></i>&nbsp; Applications</a>
        <a><i class="fa fa-cog fa-fw"></i>&nbsp; Settings</a>
      </div>
    </header>
    <body>
      <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>
      <%= yield %>
      <footer>
        <p>インスタグラムクローン</p>
        <p>© 2020 5/27 ~ 5/31 ５月期小野作成</p>
      </footer>
    </body>
</div>
</html>
